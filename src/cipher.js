window.cipher = {
  encode: () => {
    /* Acá va tu código */
/*
  const name = ingresoCC
  let result = ""
  for(let i = 0, i < name.length; i++) {
 result += name [i].toUpperCase();
 console.log(result);
  }
  
}
*/
  },
  decode: () => {
      /* Acá va tu código */
  }
}